import { FormatoModeloPipe } from './formato-modelo.pipe';

describe('FormatoModeloPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoModeloPipe();
    expect(pipe).toBeTruthy();
  });
});
