import { ModelsInputDirective } from './models-input.directive';

describe('ModelsInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ModelsInputDirective();
    expect(directive).toBeTruthy();
  });
});
